spring:
  cloud:
    gcp:
      secretmanager:
        enabled: false
      core:
        enabled: false
      sql:
        enabled: false
  flyway:
    enabled: true
  profiles:
    active: test
  application:
    name: digiplant-api
  web:
    error:
      include-message: always

eppo:
  api:
    token: tulletoken
    uri: "http://tulleuri.com"

nats:
  url: test
  creds: test
