spring:
  flyway:
    enabled: true
  profiles:
    active: test
  application:
    name: digiplant-api

eppo:
  token: tulletoken
  uri: "http://tulleuri.com"

nats:
  url: test
  creds: test

server:
  error:
    include-message: always
